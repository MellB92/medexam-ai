# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environment
.venv/
venv/
ENV/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/
*.cover
.cache
nosetests.xml
coverage.xml
*.coveragerc

# Outputs / Derived data
# ----------------------
# Hinweis (wichtig für Coding-Agents):
# Einige dieser Ordner sind für die Prüfungsvorbereitung *Inputs* (Yield-Reports, Anki-TSVs, Münster-Notes).
# Wenn sie hier pauschal ignoriert werden, melden Agents oft fälschlich "Datei nicht vorhanden",
# weil Search/Indexing git-ignorierte Pfade standardmäßig ausblendet.
#
# Deshalb: wir ignorieren nur echte GENERIERTE Outputs/Caches, die nicht versioniert werden sollen.
#
# NICHT pauschal ignorieren:
# - _GOLD_STANDARD/ (Quellen)
# - _BIBLIOTHEK/Leitlinien/ (Quellen)
# - _EXTRACTED_FRAGEN/ (kann je nach Workflow als Input gelten)
# - _PROCESSING/ (kann je nach Workflow als Input gelten)

# Generierte Outputs (groß / volatil) – NICHT committen
_OUTPUT/
_DERIVED_CHUNKS/

# Große Quellen-Bibliotheken (PDFs) – standardmäßig nicht committen
# Manifest/Metadaten (z.B. _BIBLIOTHEK/leitlinien_manifest.json) bleiben versionierbar.
_BIBLIOTHEK/Leitlinien/
_GOLD_STANDARD/
_EXTRACTED_FRAGEN/
_PROCESSING/

# External decks / large exports
_EXTERNAL_DECKS/

_LLM_ARCHIVE/

# Fact Check Sources (nur fuer Verifikation, nicht fuer Content-Generierung)
_FACT_CHECK_SOURCES/**/*.pdf
_FACT_CHECK_SOURCES/**/*.txt
_FACT_CHECK_SOURCES/**/*.json
# README.md wird versioniert fuer Dokumentation

# Temporary files
*.tmp
*.log
*.bak
*.backup
tmp_*
temp_*

# Backups (committed separately by GitHub Actions)
# backups/  # Uncomment if you don't want backups in repo

# Gold Standard
# -------------
# Nicht mehr pauschal ignorieren, damit Agents die Quellen auffinden können.
# Wenn du die Dateien nicht versionieren willst, ignoriere sie lokal (siehe Hinweis oben).

# OS specific
.DS_Store
Thumbs.db
Desktop.ini

# Jupyter Notebooks (if used)
.ipynb_checkpoints/
*.ipynb

# Environment variables
.env
.env.local
.env.*.local

# Documentation build artifacts
docs/_build/
docs/.doctrees/

# MyPy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/
.embedding_cache/

# Agent collaboration artifacts (lokal, nicht versionieren)
_AGENT_WORK/
AGENT_*.md

# Local archives / backups (nicht versionieren)
_ARCHIVE/gold_standard_backups/
_WISSENSBASIS/
# External Anki decks (copyright/size)
_EXTERNAL_DECKS/**/*.apkg

# Local exports / archives / scratch (nicht versionieren)
*.tar
*.rem
result.json

# Duplicate workspace folders (lokal)
Medexamenai_migration_full_20251217_204617 2/

# Cursor/IDE specific
.cursorignore
CURSOR_AGENT_YEAR_INFERENCE_TASK.md

# Chat/notes dumps
Konversationen mit Codingagenten/
